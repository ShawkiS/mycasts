import Button from './Button';
import Tag from './Tag';
import Cast from './Cast';
import ProfileHeader from './profileHeader';

export {
    Button,
    Tag,
    Cast,
    ProfileHeader
};

export const fieldsResolvers: Function[] = [
    Button,
    Tag,
    Cast,
    ProfileHeader
];
